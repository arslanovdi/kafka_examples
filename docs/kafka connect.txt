Обычно используетсся Debezium драйвер, который читает данные из таблицы и кладет их в топик Kafka.

Пример интеграции:
В банке было удобно читать события из Oracle (которая работала на хранимках) и класть их в Kafka.
А дальше уже в микросервисах работать с kafka.