KafkaServer {
   org.apache.kafka.common.security.plain.PlainLoginModule required
   username="admin"
   password="admin-secret"
   user_producer="producer-secret"
   user_consumer="consumer-secret";
};

/*
username и password используются для
инициации подключения брокером (межброкерская коммуникация), в моем примере не нужны ))
оставил их только в kafka3 для примера
*/
